# Slop Video Downloader

Desktop app for downloading videos (Tauri 2 + Svelte + TypeScript).

## Dev

```bash
npm install
npm run tauri dev
```

See `docs/` for design and plan. README will be expanded later.
